<h2>Добро Пожаловать в пиццерию!</h2>

<div class="row">
	<% @products.each do |p| %>
	<div class="col-md-3">
		<div>
			<%= p.title %>
		</div>

		<div>
			<img src="<%= p.path_to_image %>" width="150" />
		</div>

		<div>
			<%=p.description %>
		</div>

		<div>
			<%=p.title%>
		</div>

	</div>
	<%end%>
</div>
<br/>
<br/>

<button type="submit" class="btn btn-dark" onclick="something()">Clik</button>


